---
/**
 * Comments Component
 * Uses Turso database for comments storage.
 * Fetches comments via API and allows new comment submissions.
 */

import DatabaseComments from './DatabaseComments.astro';

interface Props {
  slug: string;
}

const { slug } = Astro.props;
---

<DatabaseComments slug={slug} />
