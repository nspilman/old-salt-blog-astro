---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import CompassRose from '../components/CompassRose.astro';
import { SITE_TITLE } from '../consts';

export const prerender = true;
---

<html lang="en">
	<head>
		<BaseHead title={`About - ${SITE_TITLE}`} description="About Old Salt Blog - a virtual port of call for all those who love the sea, hosted by nautical novelist Rick Spilman." />
	</head>
	<body>
		<Header />
		<CompassRose />
		<main>
			<article class="about-page">
				<div class="about-header">
					<h1>About Old Salt Blog</h1>
					<p class="tagline">A virtual port of call for all those who love the sea</p>
				</div>

				<div class="about-content">
					<p>
						<strong>Old Salt Blog</strong> is a virtual port of call for all those who love the sea,
						hosted by nautical novelist <strong>Rick Spilman</strong>. Since 2008, we've been sharing
						maritime news, history, and stories from around the world.
					</p>

					<p>
						From tall ships to modern cargo vessels, naval history to ocean conservation,
						we cover the full breadth of maritime topics. Whether you're a professional mariner,
						a weekend sailor, or simply someone who dreams of the sea, you'll find something
						of interest here.
					</p>

					<h2>About Rick Spilman</h2>
					<p>
						Rick Spilman is a nautical novelist and maritime enthusiast. His novels include
						<em>Hell Around the Horn</em>, a tale of a Cape Horner in the final days of the great
						sailing ships, and <em>The Shantyman</em>, based on the true story of a shantyman
						with a troubled past.
					</p>

					<h2>Connect</h2>
					<p>
						Subscribe to our <a href="/rss.xml">RSS feed</a> to stay updated with the latest posts.
					</p>
				</div>
			</article>
		</main>
		<Footer />
	</body>
</html>

<style>
	/* About page - the captain's introduction */
	main {
		max-width: 1000px;
		margin: 0 auto;
		background: linear-gradient(180deg, #FDF8F0 0%, #F5EBD8 100%);
		min-height: calc(100vh - 160px);
		position: relative;
		z-index: 2;
	}

	/* On smaller screens, go full-width */
	@media (max-width: 1040px) {
		main {
			max-width: 100%;
		}
	}

	.about-page {
		max-width: 720px;
		margin: 0 auto;
		padding: 1.5rem 1rem;
	}

	.about-header {
		margin-bottom: 2rem;
		padding-bottom: 1rem;
		border-bottom: 2px solid var(--border-dark);
	}

	h1 {
		font-size: 2rem;
		margin: 0 0 0.25rem 0;
		color: rgb(var(--black));
	}

	.tagline {
		font-size: 1rem;
		color: rgb(var(--gray));
		margin: 0;
		font-style: italic;
		font-weight: normal;
	}

	.about-content {
		line-height: 1.7;
		color: rgb(var(--gray-dark));
	}

	.about-content p {
		margin-bottom: 1.25rem;
	}

	.about-content h2 {
		font-size: 1.5rem;
		margin: 2rem 0 0.75rem;
		color: rgb(var(--black));
	}

	/* Links - bold blue, the soul */
	.about-content a {
		color: var(--accent);
		font-weight: 700;
		text-decoration: none;
	}

	.about-content a:hover {
		text-decoration: underline;
	}

	/* Tablet */
	@media (max-width: 768px) {
		.about-page {
			padding: 1.25rem 1rem;
		}

		.about-header {
			margin-bottom: 1.5rem;
			padding-bottom: 0.875rem;
		}

		h1 {
			font-size: 1.5rem;
		}

		.tagline {
			font-size: 0.9375rem;
		}

		.about-content h2 {
			font-size: 1.25rem;
			margin: 1.5rem 0 0.625rem;
		}

		.about-content p {
			margin-bottom: 1rem;
		}
	}

	/* Small mobile */
	@media (max-width: 480px) {
		.about-page {
			padding: 1rem 0.875rem;
		}

		h1 {
			font-size: 1.375rem;
		}

		.tagline {
			font-size: 0.875rem;
		}

		.about-content {
			font-size: 0.9375rem;
		}

		.about-content h2 {
			font-size: 1.125rem;
			margin: 1.25rem 0 0.5rem;
		}
	}
</style>
