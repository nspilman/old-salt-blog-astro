# TinaCMS Configuration
# Get these from https://tina.io after creating a project
# Required for production builds and cloud editing
TINA_CLIENT_ID=your-client-id-here
TINA_TOKEN=your-token-here

# Optional: Git branch to use (defaults to main)
# GITHUB_BRANCH=main

# AWS S3 Configuration
# Required for migrating WordPress media to S3 (scripts/migrate/media-to-s3.ts)
# Get credentials from AWS IAM: https://console.aws.amazon.com/iam/
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1

# Note: The S3 bucket should have public read access for images to be viewable on the site
# Recommended: Set up CloudFront CDN in front of S3 for better performance
